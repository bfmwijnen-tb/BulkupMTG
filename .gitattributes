# Auto detect text files and perform LF normalization
* text=auto

# The standalone page is generated by `cargo run --bin bundle`. Marking it
# keeps GitHub from reporting the repo as mostly HTML and from rendering a
# multi-megabyte diff on every regeneration.
bulkup.html linguist-generated=true -diff
